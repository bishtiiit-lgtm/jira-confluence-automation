import { createApp } from './app.js';
import { loadEnvironment } from './config/environment.js';

const env = loadEnvironment();
const app = createApp();

app.listen(env.port, () => {
  console.log(`Delivery Risk backend listening on port ${env.port}`);
});
